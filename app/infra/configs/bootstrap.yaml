name: infra-service
version: 1.0.0
mode: dev

registry:
  etcd:
    endpoints:
      - "192.168.1.6:2379"
    username: "root"
    password: "123456"
    timeout: 5s

config:
  etcd: "/config/infra/config.yaml"

log:
  level: debug
  file: logs/infra-service.log