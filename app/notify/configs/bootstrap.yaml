name: notify-service
version: 1.0.0
mode: dev

registry:
  consul:
    address: "127.0.0.1:8500"
    token: "your-token"

config:
  path: "/config/notify/config.yaml"

log:
  level: debug
  file: logs/notify-service.log