syntax = "proto3";

package common.api.common.v1;

option go_package = "common/gen/common/v1;v1";

// System 服务
service System {
  // 健康检查
  rpc Health (HealthRequest) returns (HealthReply);
}

message HealthRequest {
}

message HealthReply {
  // 响应消息
  string message = 1;
}
