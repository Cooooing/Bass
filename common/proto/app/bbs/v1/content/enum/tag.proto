syntax = "proto3";

package common.api.app.bbs.v1.content;

option go_package = "common/proto/gen/bbs/v1/content/enum;enum";

// 标签状态。
enum TagStatus {
  TAG_STATUS_UNSPECIFIED = 0; // 未指定
  TAG_STATUS_ENABLED = 1; // 启用
  TAG_STATUS_DISABLED = 2; // 停用
}
