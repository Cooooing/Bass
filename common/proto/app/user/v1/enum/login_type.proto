syntax = "proto3";

package common.api.app.user.v1;

option go_package = "common/proto/gen/user/v1/enum;enum";

// LoginType 表示登录方式。
enum LoginType {
  LOGIN_TYPE_UNSPECIFIED = 0; // 未指定
  LOGIN_TYPE_PASSWORD = 1; // 密码登录
  LOGIN_TYPE_EMAIL = 2; // 邮箱验证码登录
  LOGIN_TYPE_PHONE = 3; // 手机验证码登录
}