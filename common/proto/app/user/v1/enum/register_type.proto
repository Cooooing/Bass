syntax = "proto3";

package common.api.app.user.v1;

option go_package = "common/proto/gen/user/v1/enum;enum";

// RegisterType 表示账号注册方式。
enum RegisterType {
  REGISTER_TYPE_UNSPECIFIED = 0; // 未指定
  REGISTER_TYPE_EMAIL = 1; // 邮箱注册
  REGISTER_TYPE_PHONE = 2; // 手机号注册
}