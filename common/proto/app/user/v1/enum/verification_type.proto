syntax = "proto3";

package common.api.app.user.v1;

option go_package = "common/proto/gen/user/v1/enum;enum";

// VerificationType 表示验证码账号类型。
enum VerificationType {
  VERIFICATION_TYPE_UNSPECIFIED = 0; // 未指定
  VERIFICATION_TYPE_EMAIL = 1; // 邮箱验证码
  VERIFICATION_TYPE_PHONE = 2; // 手机验证码
}
